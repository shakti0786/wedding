{
  "name": "wedding",
  "version": "1.0.0",
  "description": "Wedding website for SAYSM",
  "scripts": {
    "build": "gulp build",
    "deploy": "gulp deploy",
    "serve": "gulp serve"
  },
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-sass": "^2.2.0"
  },
  "dependencies": {
    "moment": "^2.11.2"
  }
}
